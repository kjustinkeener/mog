{
  "name": "Transpose a CSV table",
  "description": "Swap the rows and columns of a CSV (or other delimited) table: the first row becomes the first column, and so on. Quote-aware; jagged rows are padded to the widest row. Set 'delimiter' for TSV or pipe-delimited input.",
  "summary": "Swap the rows and columns of a CSV table",
  "tags": ["csv", "reshape", "table", "data"],
  "steps": [
    { "action": "transpose", "description": "Rows become columns and vice versa" }
  ]
}
