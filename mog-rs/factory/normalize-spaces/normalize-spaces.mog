{
  "name": "Normalize runs of spaces",
  "description": "Collapse runs of spaces and tabs down to a single space on each line (line breaks are kept). Cleans up text that was aligned or padded with extra spaces. To also trim leading/trailing space, follow with trim_whitespace.",
  "summary": "Collapse runs of spaces and tabs to one space",
  "tags": [
    "whitespace",
    "normalize",
    "cleanup",
    "text"
  ],
  "steps": [
    {
      "action": "collapse_whitespace",
      "description": "Collapse space/tab runs to a single space"
    }
  ]
}
